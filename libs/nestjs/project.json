{
  "name": "nestjs",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/nestjs/src",
  "projectType": "library",
  "tags": [],
  "// targets": "to see all targets run: nx show project nestjs --web",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "cache": true,
      "outputs": ["{workspaceRoot}/dist/libs/nestjs"],
      "options": {
        "command": "npx tsc -p libs/nestjs/tsconfig.lib.json",
        "cwd": "{workspaceRoot}"
      }
    }
  }
}

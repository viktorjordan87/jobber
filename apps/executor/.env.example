# Pulsar must be reachable (e.g. `npm run docker:up` — see docker/docker.compose.yml `pulsar` service on 6650).
PORT=3002
COOKIE_SECRET=secret
PULSAR_SERVICE_URL=pulsar://localhost:6650
JOBS_GRPC_SERVICE_URL=localhost:5002
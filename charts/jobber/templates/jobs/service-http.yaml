{{- if .Values.jobs.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: jobs
  labels:
    app: jobs
spec:
  type: ClusterIP
  ports:
    - protocol: TCP
      port: {{ .Values.jobs.port.http }}
      targetPort: {{ .Values.jobs.port.http }}
  selector:
    app: jobs
{{- end }}